{
  "name": "mario-backend",
  "version": "1.0.0",
  "main": "index.js",  // Make sure this points to your backend entry point
  "scripts": {
    "start": "node index.js"  // This ensures Vercel starts the backend correctly
  },
  "dependencies": {
    "express": "^4.17.1",
    "cors": "^2.8.5",
    "googleapis": "^78.0.0"
  }
}
